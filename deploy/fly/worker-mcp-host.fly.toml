app = "hive-worker-mcp-host"
primary_region = "ord"

[build]
  dockerfile = "Dockerfile.ts-app"

[build.args]
  SERVICE = "workers/mcp_host"

[env]
  HIVE_WORKER_REGION = "fly-ord"
  HIVE_WORKER_ZONE   = "default"
  HIVE_ENV_LABEL     = "production"

[[vm]]
  cpu_kind = "shared"
  cpus     = 1
  memory   = "512mb"
