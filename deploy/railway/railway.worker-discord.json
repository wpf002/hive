{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "deploy/fly/Dockerfile.python-worker",
    "dockerfileBuildArgs": { "WORKER": "discord" }
  },
  "deploy": {
    "startCommand": "hive-discord",
    "restartPolicyType": "ON_FAILURE"
  }
}
