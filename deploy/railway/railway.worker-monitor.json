{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "deploy/fly/Dockerfile.python-worker",
    "dockerfileBuildArgs": { "WORKER": "monitor" }
  },
  "deploy": {
    "startCommand": "hive-monitor",
    "restartPolicyType": "ON_FAILURE"
  }
}
