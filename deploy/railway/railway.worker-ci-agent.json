{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "deploy/fly/Dockerfile.python-worker",
    "dockerfileBuildArgs": { "WORKER": "ci_agent" }
  },
  "deploy": {
    "startCommand": "hive-ci-agent",
    "restartPolicyType": "ON_FAILURE"
  }
}
