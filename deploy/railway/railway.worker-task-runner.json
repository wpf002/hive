{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "deploy/fly/Dockerfile.python-worker",
    "dockerfileBuildArgs": { "WORKER": "task_runner" }
  },
  "deploy": {
    "startCommand": "hive-task-runner",
    "restartPolicyType": "ON_FAILURE"
  }
}
