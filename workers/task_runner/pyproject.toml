[project]
name = "hive-task-runner"
version = "0.1.0"
description = "Generic distributed tasks (arq)"
requires-python = ">=3.11"
dependencies = [
    "hive-base",
    "arq>=0.26.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
