[project]
name = "hive-monitor"
version = "0.1.0"
description = "Uptime / health checks / alerting"
requires-python = ">=3.11"
dependencies = [
    "hive-base",
    "apscheduler>=3.10.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
